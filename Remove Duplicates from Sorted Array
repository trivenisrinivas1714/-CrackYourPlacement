```python
class Solution:
    def removeDuplicates(self, arr: List[int]) -> int:
        s = set()
        for i in range(1,len(arr)):
            s.add(arr[i])
        k = len(s)
        j = 0
        for a in s:
            arr[j] = a
            j += 1
        return k
